spring:
  application:
    name: ToolVerse Backend
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb+srv://vineethnaikeslavath_db_user:hsj7XeL5tRhD3oAu@toolverse.ovqjujb.mongodb.net/?appName=ToolVerse}
      database: ${SPRING_DATA_MONGODB_DATABASE:toolverse}
  mail:
    host: ${SPRING_MAIL_HOST:smtp.gmail.com}
    port: ${SPRING_MAIL_PORT:587}
    username: ${SPRING_MAIL_USERNAME:your-email@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:your-app-password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

logging:
  level:
    com.toolverse: DEBUG
    org.springframework.data.mongodb: DEBUG

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:mySecretKey123456789012345678901234567890}
  expiration: 86400000

# Email Verification
verification:
  token:
    expiration: 3600000 # 1 hour